<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=0.8">

    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/components.css">
    <link rel="stylesheet" href="/css/pages.css">
</head>

<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
            </ul>
        </nav>

        <div class="logo">BNB ADVISOR</div>

        <nav>
            <ul>
                {{#if user}}
                    <li class="nav-user">
                        <a href="/users/profile">Welcome, {{user.firstName}}!</a>
                        {{#if (eq user.role 'manager')}}
                            <span class="user-role">Host</span>
                        {{else}}
                            <span class="user-role">Guest</span>
                        {{/if}}
                    </li>
                    <li>
                        <form method="POST" action="/users/logout" style="display: inline;">
                            <button type="submit" class="logout-btn">Logout</button>
                        </form>
                    </li>
                {{else}}
                    <li><a href="/users/login">Sign In</a></li>
                    <li><a href="/users/signup">Sign Up</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>

    {{{body}}}
    <footer>CS546 FINAL PROJECT</footer>
    
    <!-- JavaScript files -->
    <script src="/js/auth.js"></script>

</body>

</html>